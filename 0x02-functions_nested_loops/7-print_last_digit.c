#include "main.h"
/**
 * print_last_digit - prints last digit of a number
 * @n: Formal parameter
 * Return: 0
 */
int print_last_digit(int n)
{
	int last_digit;

	last_digit = n % 10;
	return (last_digit);
}
